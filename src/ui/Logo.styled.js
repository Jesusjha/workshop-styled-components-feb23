import styled from "styled-components";

export const Logo = styled.h1`
  font-family: 'Fredericka the Great', cursive;
  font-size: 2.8rem;
  text-transform: uppercase;
  line-height: 3rem;
`;

export const LogoSpan = styled(Logo)`
  font-size: 4.2rem;
  text-transform: capitalize;
`